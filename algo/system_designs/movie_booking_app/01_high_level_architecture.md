# High-Level Architecture (AI/ML-Powered Hybrid System)

## System Overview Diagram

```
                AI/ML-POWERED MOVIE TICKET BOOKING SYSTEM
                     (Hybrid Architecture)

┌─────────────────────────────────────────────────────────────────────────┐
│                            CLIENT LAYER                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │
│   │  Web Browser │  │  Mobile App  │  │  Admin Panel │               │
│   │   (React)    │  │ (iOS/Android)│  │   (Vue.js)   │               │
│   │              │  │              │  │              │               │
│   │ • NL Search  │  │ • AI Chatbot │  │ • Pricing AI │               │
│   │ • ML Recom   │  │ • Voice UI   │  │ • Analytics  │               │
│   └──────┬───────┘  └──────┬───────┘  └──────┬───────┘               │
│          │                  │                  │                        │
└──────────┼──────────────────┼──────────────────┼────────────────────────┘
           │                  │                  │
           └──────────────────┴──────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                         EDGE LAYER                                      │
├─────────────────────────────────────────────────────────────────────────┤
│   ┌───────────────────────────────────────────────────────────────┐   │
│   │  CDN (CloudFront/Cloudflare)                                   │   │
│   │  • Static assets, Movie posters                               │   │
│   │  • ML model artifacts (cached for edge inference)             │   │
│   └───────────────────────────────────────────────────────────────┘   │
│                              ↓                                          │
│   ┌───────────────────────────────────────────────────────────────┐   │
│   │  Load Balancer + API Gateway (Kong)                           │   │
│   │  • Rate limiting (per-user, per-IP)                           │   │
│   │  • JWT validation                                             │   │
│   │  • Request routing (traditional vs AI endpoints)              │   │
│   └───────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                   TRADITIONAL SERVICES LAYER                            │
│                   (Deterministic, Low Latency, High Reliability)        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐          │
│  │ Movie Service  │  │Theater Service │  │ Search Service │          │
│  │  (Port 8001)   │  │  (Port 8002)   │  │  (Port 8003)   │          │
│  │                │  │                │  │                │          │
│  │ • CRUD movies  │  │ • Theaters     │  │ • Elasticsearch│          │
│  │ • Reviews      │  │ • Shows        │  │ • Basic search │          │
│  │ • Ratings      │  │ • Seat layout  │  │ • Filters      │          │
│  └────────┬───────┘  └────────┬───────┘  └────────┬───────┘          │
│           │                   │                   │                    │
│  ┌────────┴───────┐  ┌────────┴───────┐  ┌────────┴───────┐          │
│  │ Booking Service│  │Payment Service │  │  User Service  │          │
│  │  (Port 8004)   │  │  (Port 8005)   │  │  (Port 8006)   │          │
│  │   ⭐ CRITICAL  │  │   ⭐ CRITICAL  │  │                │          │
│  │                │  │                │  │ • Auth/Profile │          │
│  │ • Lock seat    │  │ • Stripe/PayPal│  │ • History      │          │
│  │ • Create order │  │ • Refunds      │  │ • Preferences  │          │
│  │ • Confirm/     │  │ • Webhooks     │  │                │          │
│  │   Cancel       │  │                │  │                │          │
│  └────────┬───────┘  └────────┬───────┘  └────────┬───────┘          │
│           │                   │                   │                    │
└───────────┼───────────────────┼───────────────────┼────────────────────┘
            │                   │                   │
            └───────────────────┴───────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                      AI/ML SERVICES LAYER (NEW!)                        │
│                   (Intelligent, Adaptive, Higher Latency OK)            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌────────────────────────────┐  ┌────────────────────────────────┐  │
│  │ Recommendation Service      │  │  Dynamic Pricing Service       │  │
│  │   (Port 8101)               │  │    (Port 8102)                 │  │
│  │                             │  │                                │  │
│  │ • Collaborative filtering   │  │  • XGBoost/Neural Net model    │  │
│  │ • Neural Collaborative      │  │  • Features: occupancy,        │  │
│  │   Filtering (NCF)           │  │    time_to_show, demand        │  │
│  │ • Content-based filtering   │  │  • Real-time + batch modes     │  │
│  │ • Hybrid recommendations    │  │  • A/B testing framework       │  │
│  │ • Cold start handling       │  │  • Revenue optimization        │  │
│  │                             │  │                                │  │
│  │ Models:                     │  │  Models:                       │  │
│  │  - User embeddings (256D)   │  │   - Pricing predictor          │  │
│  │  - Movie embeddings (256D)  │  │   - Demand forecaster          │  │
│  │  - NCF deep model           │  │                                │  │
│  └─────────────────────────────┘  └────────────────────────────────┘  │
│           │                                    │                        │
│           ↓                                    ↓                        │
│  ┌────────────────────────────┐  ┌────────────────────────────────┐  │
│  │ AI Chatbot Service          │  │  Fraud Detection Service       │  │
│  │   (Port 8103)               │  │    (Port 8104)                 │  │
│  │                             │  │                                │  │
│  │ • LLM Integration:          │  │  • Real-time scoring (<50ms)   │  │
│  │   - GPT-4 (complex queries) │  │  • Isolation Forest model      │  │
│  │   - GPT-3.5 (simple)        │  │  • Autoencoder for anomalies   │  │
│  │ • Function calling:         │  │                                │  │
│  │   - search_movies()         │  │  Features:                     │  │
│  │   - check_availability()    │  │   - Booking velocity           │  │
│  │   - create_booking()        │  │   - Failed payments            │  │
│  │   - cancel_booking()        │  │   - Device fingerprint         │  │
│  │ • Context management        │  │   - Location anomalies         │  │
│  │ • Conversation history      │  │   - Time patterns              │  │
│  │ • Cost optimization         │  │                                │  │
│  │   (caching, model tiering)  │  │  Actions: Block/Verify/Allow   │  │
│  └─────────────────────────────┘  └────────────────────────────────┘  │
│           │                                    │                        │
│           ↓                                    ↓                        │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │         Personalization Engine (Port 8105)                       │  │
│  │                                                                  │  │
│  │  • User behavior tracking                                        │  │
│  │  • Preference learning                                           │  │
│  │  • Session-based recommendations                                 │  │
│  │  • Real-time feature computation                                 │  │
│  │  • A/B experiment framework                                      │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  All AI/ML services: Auto-scaling (2-20 pods), GPU-enabled            │
│  Fallback: If ML service fails → rule-based system                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    ML INFRASTRUCTURE LAYER (NEW!)                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Feature Store (Feast / Tecton)                           │        │
│  │  • Online features (real-time, <10ms)                     │        │
│  │  • Offline features (batch, for training)                 │        │
│  │  • Feature versioning                                     │        │
│  │  • Point-in-time correctness                              │        │
│  │                                                            │        │
│  │  Features:                                                 │        │
│  │    User: watch_history, preferences, booking_frequency    │        │
│  │    Movie: genre, cast, ratings, popularity                │        │
│  │    Context: time, location, device                        │        │
│  └───────────────────────────────────────────────────────────┘        │
│                              ↓                                          │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Model Serving (TensorFlow Serving / TorchServe)         │        │
│  │  • Model versioning (A/B test different versions)         │        │
│  │  • Batch inference                                        │        │
│  │  • GPU acceleration                                       │        │
│  │  • Model monitoring (drift detection)                     │        │
│  │  • Auto-scaling based on load                             │        │
│  └───────────────────────────────────────────────────────────┘        │
│                              ↓                                          │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  ML Orchestration (Kubeflow / MLflow)                     │        │
│  │  • Model training pipelines                               │        │
│  │  • Experiment tracking                                    │        │
│  │  • Model registry                                         │        │
│  │  • Automated retraining                                   │        │
│  └───────────────────────────────────────────────────────────┘        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                        DATA LAYER                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Cache Layer (Redis Cluster)                              │        │
│  │  • Seat availability (TTL: 30s)                           │        │
│  │  • Movie details (TTL: 1h)                                │        │
│  │  • User embeddings (TTL: 1h)  ← ML feature cache         │        │
│  │  • LLM response cache (TTL: 1 day)  ← Cost optimization  │        │
│  │  • Session storage                                        │        │
│  │  • Distributed locks (seat booking)                       │        │
│  └───────────────────────────────────────────────────────────┘        │
│                              ↓                                          │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Primary Database (PostgreSQL)                            │        │
│  │  • Users, Movies, Theaters, Shows, Bookings               │        │
│  │  • ML training data (bookings, ratings, clicks)           │        │
│  │  • ACID compliance for booking                            │        │
│  └────────────────────┬──────────────────────────────────────┘        │
│                       │                                                 │
│       ┌───────────────┴───────────────┐                               │
│       ↓                               ↓                               │
│  ┌─────────────┐               ┌─────────────┐                       │
│  │ Read Replica│               │ Read Replica│                       │
│  │  (Queries)  │               │  (ML Train) │ ← For ML training     │
│  └─────────────┘               └─────────────┘                       │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Vector Database (Pinecone / Weaviate / Milvus)  (NEW!)  │        │
│  │  • User embeddings (256D)                                 │        │
│  │  • Movie embeddings (256D)                                │        │
│  │  • Similarity search for recommendations                  │        │
│  │  • ANN (Approximate Nearest Neighbors) - <50ms           │        │
│  └───────────────────────────────────────────────────────────┘        │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Graph Database (Neo4j)  (NEW!)                           │        │
│  │  • User social connections                                │        │
│  │  • "Friends who watched" recommendations                  │        │
│  │  • Collaborative filtering graph                          │        │
│  └───────────────────────────────────────────────────────────┘        │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Time-Series DB (InfluxDB / TimescaleDB)  (NEW!)         │        │
│  │  • User behavior events (clicks, views, bookings)         │        │
│  │  • Model performance metrics                              │        │
│  │  • Real-time analytics                                    │        │
│  └───────────────────────────────────────────────────────────┘        │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Search Engine (Elasticsearch)                            │        │
│  │  • Movie search (traditional keyword)                     │        │
│  │  • NLP-powered search (semantic understanding)  (NEW!)    │        │
│  │  • Autocomplete suggestions                               │        │
│  └───────────────────────────────────────────────────────────┘        │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Data Warehouse (BigQuery / Snowflake)  (NEW!)           │        │
│  │  • ML training datasets                                   │        │
│  │  • Business analytics                                     │        │
│  │  • A/B test results                                       │        │
│  └───────────────────────────────────────────────────────────┘        │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Object Storage (S3)                                      │        │
│  │  • Movie posters                                          │        │
│  │  • ML model artifacts  (NEW!)                             │        │
│  │  • Training datasets  (NEW!)                              │        │
│  │  • Logs backup                                            │        │
│  └───────────────────────────────────────────────────────────┘        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    MESSAGING & EVENT LAYER                              │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐        │
│  │  Message Queue (Apache Kafka)                             │        │
│  │                                                            │        │
│  │  Traditional Topics:                                       │        │
│  │    • booking.created, booking.confirmed                    │        │
│  │    • payment.completed, payment.failed                     │        │
│  │    • seat.locked, seat.released                            │        │
│  │                                                            │        │
│  │  AI/ML Topics: (NEW!)                                      │        │
│  │    • user.action (click, view, search)  ← Real-time ML     │        │
│  │    • model.prediction (for logging)                        │        │
│  │    • recommendation.shown, recommendation.clicked          │        │
│  │    • price.updated (dynamic pricing events)                │        │
│  │    • fraud.detected (alerts)                               │        │
│  │                                                            │        │
│  │  Consumers:                                                │        │
│  │    • Notification Service                                  │        │
│  │    • Analytics Service                                     │        │
│  │    • ML Feature Store (real-time features)  (NEW!)         │        │
│  │    • Model Monitoring Service  (NEW!)                      │        │
│  └───────────────────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Traditional:                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                │
│  │Payment (Str) │  │  SMS (Twilio)│  │Email(SendGr) │                │
│  └──────────────┘  └──────────────┘  └──────────────┘                │
│                                                                         │
│  AI/ML (NEW!):                                                          │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────────┐   │
│  │ OpenAI API       │  │  Anthropic Claude│  │  HuggingFace      │   │
│  │ (LLM for chatbot)│  │  (Alternative LLM)│  │  (Open models)    │   │
│  └──────────────────┘  └──────────────────┘  └───────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                  MONITORING & OBSERVABILITY                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Traditional Monitoring:                                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                │
│  │  Prometheus  │  │   Grafana    │  │   ELK Stack  │                │
│  │  (Metrics)   │  │ (Dashboards) │  │   (Logs)     │                │
│  └──────────────┘  └──────────────┘  └──────────────┘                │
│                                                                         │
│  ML-Specific Monitoring (NEW!):                                         │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────────┐   │
│  │ Evidently AI     │  │  Weights & Biases│  │  MLflow           │   │
│  │ (Model drift)    │  │  (Experiments)   │  │  (Model tracking) │   │
│  └──────────────────┘  └──────────────────┘  └───────────────────┘   │
│                                                                         │
│  Key ML Metrics:                                                        │
│  • Model latency (p50, p95, p99)                                       │
│  • Prediction accuracy (online A/B tests)                              │
│  • Feature freshness                                                    │
│  • Recommendation CTR, conversion                                       │
│  • LLM cost per query                                                   │
│  • Fraud detection TPR/FPR                                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## Critical Data Flows

### Flow 1: Traditional Booking (CRITICAL PATH - No AI/ML!)

```
User selects seats → Booking Service
  ↓
1. Acquire distributed lock (Redis)
   SET lock:seat:{show_id}:{seat_ids} user_id NX EX 300
  ↓
2. Check seat availability (PostgreSQL with row lock)
   SELECT * FROM seats WHERE id IN (...) FOR UPDATE
  ↓
3. Update seat status = 'LOCKED'
  ↓
4. Create booking (status = 'PENDING')
  ↓
5. Payment Service → Stripe
  ↓
6. On payment success:
   - Update booking status = 'CONFIRMED'
   - Update seat status = 'BOOKED'
   - Release distributed lock
   - Publish Kafka event: booking.confirmed
  ↓
7. Kafka → Notification Service
   - Send email/SMS confirmation
  ↓
8. Kafka → ML Services (async, non-blocking)
   - Log event for ML training
   - Update user behavior tracking
   - Trigger recommendation refresh

Total latency target: <2 seconds
```

### Flow 2: AI Chatbot Query (AI/ML Enhanced)

```
User: "Show me action movies near downtown tonight"
  ↓
Chatbot Service receives query
  ↓
1. Check LLM response cache (Redis)
   - Cache key: hash(query + user_context)
   - Cache hit? → Return immediately (saves cost!)
  ↓
2. Cache miss → Call LLM API
   ↓
   LLM (GPT-4) with function calling:
   {
     "function": "search_movies",
     "arguments": {
       "genre": "action",
       "location": "downtown",
       "date": "2024-01-15",
       "time": "evening"
     }
   }
  ↓
3. Execute function: Movie Service.search()
   - Query Elasticsearch with filters
   - Returns: List of movies + theaters
  ↓
4. Call Recommendation Service (parallel)
   - Personalize results based on user history
   - Rerank movies using ML model
  ↓
5. LLM formats response:
   "I found 5 action movies near downtown tonight:
    1. John Wick 4 at AMC Downtown (7PM, 9:30PM)
    2. Fast X at Regal Cinema (7:15PM, 10PM)
    ..."
  ↓
6. Cache response (TTL: 1 hour)
  ↓
7. Log interaction (Kafka)
   - For ML model improvement
   - For cost tracking
  ↓
Return to user

Total latency: 1-3 seconds (acceptable for chat)
```

### Flow 3: Personalized Recommendations (AI/ML)

```
User visits homepage
  ↓
Frontend requests: GET /api/recommendations?user_id=123
  ↓
Recommendation Service
  ↓
1. Fetch user embedding from cache (Redis)
   - Cache hit? → Use cached
   - Cache miss? → Compute from Feature Store
  ↓
2. Feature Store fetches:
   - User features: watch_history, preferences, demographics
   - Context features: time_of_day, device, location
  ↓
3. Two-stage recommendation:

   Stage 1: Candidate Generation (Fast!)
   ────────────────────────────────────
   • Vector DB similarity search (Pinecone)
   • Query: user_embedding
   • Return: Top 100 similar movies (ANN search, <50ms)

   Stage 2: Ranking (Slower but accurate)
   ──────────────────────────────────────
   • ML model (Neural Collaborative Filtering)
   • Input: user_features + movie_features + context
   • Output: Predicted rating for each movie
   • Sort by predicted rating
   • Return: Top 10
  ↓
4. Apply business rules:
   - Filter out already watched
   - Ensure diversity (different genres)
   - Apply A/B test variants
  ↓
5. Log recommendations (Kafka)
   - Track what was shown (for CTR measurement)
  ↓
6. Cache result (TTL: 1 hour per user)
  ↓
Return to frontend

Total latency: <500ms
```

### Flow 4: Dynamic Pricing (ML-Based)

```
Background Job (runs every 10 minutes)
  ↓
Dynamic Pricing Service
  ↓
1. For each upcoming show (next 7 days):
   ↓
   Fetch features from Feature Store:
   - current_occupancy_rate
   - time_to_show_hours
   - day_of_week
   - movie_popularity_score
   - historical_demand (same slot last week)
   - competitor_pricing (web scraping)
   - weather_forecast
   - nearby_events
  ↓
2. ML Model Inference (XGBoost):
   Input: feature vector (8-10 features)
   Output: optimal_price
  ↓
3. Apply business constraints:
   - min_price = base_price * 0.7
   - max_price = base_price * 2.0
   - max_change_per_update = 10%
  ↓
4. If price changed:
   - Update database
   - Invalidate cache
   - Publish Kafka event: price.updated
  ↓
5. A/B test tracking:
   - Control group: rule-based pricing
   - Treatment group: ML-based pricing
   - Measure: revenue per seat
  ↓
6. Log prediction (for model monitoring)
   - Actual revenue vs predicted
   - Detect model drift

Batch mode: Process 50K shows in 5 minutes
```

### Flow 5: Real-time Fraud Detection (ML)

```
User completes booking → Payment Service
  ↓
BEFORE processing payment:
  ↓
Fraud Detection Service (Real-time scoring!)
  ↓
1. Extract features:
   - booking_velocity: # bookings last hour
   - failed_payment_count: last 24 hours
   - device_fingerprint: browser, OS, screen size
   - location_anomaly: current vs historical locations
   - unusual_time: booking at 3 AM?
   - vip_show_pattern: always books expensive shows
   - email_domain_reputation
  ↓
2. Fetch from Feature Store:
   - User historical features (batch)
   - Real-time aggregations (stream)
  ↓
3. ML Model Inference (Isolation Forest):
   Input: feature vector (10-15 features)
   Output: fraud_score (0-1)
  ↓
4. Decision logic:
   - fraud_score > 0.8 → BLOCK (high risk)
   - 0.5 < fraud_score < 0.8 → VERIFY (require 2FA)
   - fraud_score < 0.5 → ALLOW (low risk)
  ↓
5. If BLOCK:
   - Return error to user
   - Alert fraud team
   - Log to Kafka
  ↓
6. If VERIFY:
   - Trigger 2FA flow
   - Hold booking
  ↓
7. If ALLOW:
   - Continue with payment
  ↓
8. Feedback loop:
   - Fraud analyst reviews flagged cases
   - Labels as true/false positive
   - Retrain model weekly with new labels

Latency requirement: <50ms (part of payment flow!)
```

## Hybrid Architecture Decision Tree

```
┌────────────────────────────────────────────────────┐
│  For each feature, ask:                            │
├────────────────────────────────────────────────────┤
│                                                    │
│  Q1: Does it involve money/booking?                │
│      ├─ YES → Use TRADITIONAL (deterministic)     │
│      └─ NO  → Go to Q2                            │
│                                                    │
│  Q2: Does it need <100ms latency?                  │
│      ├─ YES → Use TRADITIONAL (fast)              │
│      └─ NO  → Go to Q3                            │
│                                                    │
│  Q3: Can it tolerate occasional failures?          │
│      ├─ YES → Consider AI/ML (enhancement)        │
│      └─ NO  → Use TRADITIONAL (reliable)          │
│                                                    │
│  Q4: Does it improve user experience?              │
│      ├─ YES → Use AI/ML (personalization)         │
│      └─ NO  → Use TRADITIONAL                     │
│                                                    │
└────────────────────────────────────────────────────┘

Examples:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Seat Booking      → Q1: YES → TRADITIONAL ✅
Payment           → Q1: YES → TRADITIONAL ✅
Recommendations   → Q1: NO, Q2: NO, Q3: YES, Q4: YES → AI/ML ✅
Dynamic Pricing   → Q1: NO, Q2: NO, Q3: YES, Q4: YES → AI/ML ✅
Chatbot           → Q1: NO, Q2: NO, Q3: YES, Q4: YES → AI/ML ✅
Fraud Detection   → Q1: YES, Q2: YES, Q3: NO → Hybrid! ⚠️
                    (ML scoring + rule-based backup)
```

## Interview Discussion Points

### 1. Why Not Use AI/ML for Booking?

**Bad Idea:**
```
❌ ML model decides seat locking strategy
❌ AI optimizes payment flow
❌ LLM handles booking transactions
```

**Why Bad:**
- Non-deterministic (models can fail unpredictably)
- Higher latency (100ms+ for inference)
- Expensive (GPU costs)
- Regulatory risk (financial transactions need auditability)
- Cannot afford "hallucinations"

**Good Approach:**
```
✅ Traditional booking with locks
✅ ML enhances UX (recommendations, pricing)
✅ Always have fallbacks for ML failures
```

### 2. Cost Optimization for LLM Chatbot

**Without Optimization:**
- 1M queries/day
- Average tokens: 500 per query
- GPT-4: $0.03/1K tokens
- Cost: $15,000/day = $450K/month 💸

**With Optimization:**
```
1. Response Caching (80% hit rate)
   - Only 200K actual LLM calls
   - Cost reduced to $90K/month

2. Model Tiering
   - Simple queries (70%): GPT-3.5 ($0.002/1K) → $2.8K
   - Complex queries (30%): GPT-4 ($0.03/1K) → $18K
   - Total: $20.8K/month

3. Prompt Engineering
   - Reduce tokens per query: 500 → 200
   - Cost: $8.3K/month

Final cost: ~$10K/month (95% savings!)
```

### 3. ML Model Serving at Scale

**Challenge**: 50K recommendations requests/second at peak

**Solution: Two-stage Ranking**

```
Stage 1: Candidate Generation (CPU-based)
  • Fast ANN search in vector DB
  • Retrieves top 100 candidates
  • Latency: <50ms
  • Cost: Low (no GPU needed)

Stage 2: Precise Ranking (GPU-based)
  • Neural network reranking
  • Only on top 100 (not entire catalog)
  • Batching: Group 32 requests
  • Latency: <100ms per batch
  • Cost: Moderate (GPU efficient with batching)

Total latency: <150ms
Throughput: 50K QPS with 20 GPU instances
```

### 4. When ML Model Fails - Graceful Degradation

```
┌─────────────────────────────────────────────────────┐
│  ML Service Failure Scenarios                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Recommendation Service Down:                       │
│    ↓                                                │
│  Fallback: Popularity-based recommendations        │
│    • Show top-rated movies                          │
│    • Genre-based (if user has preference)          │
│    • Acceptable degraded experience                │
│                                                     │
│  Dynamic Pricing Service Down:                      │
│    ↓                                                │
│  Fallback: Default pricing table                   │
│    • Base price + simple rules                     │
│    • Peak hour: +20%                               │
│    • Weekend: +15%                                 │
│    • Still functional                               │
│                                                     │
│  Fraud Detection Model Down:                        │
│    ↓                                                │
│  Fallback: Rule-based fraud detection              │
│    • Check velocity thresholds                     │
│    • Blacklist known bad actors                    │
│    • More false positives, but safe                │
│                                                     │
│  AI Chatbot Down:                                   │
│    ↓                                                │
│  Fallback: Traditional search + FAQ                │
│    • Show search bar                               │
│    • Link to FAQ                                   │
│    • Option to contact human support               │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 5. Monitoring ML Models in Production

**Key Metrics Dashboard:**

```
┌──────────────────────────────────────────────────────┐
│  ML Model Health Dashboard                           │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Recommendation Model:                               │
│  ✅ Latency: p95 = 87ms (target: <100ms)            │
│  ✅ CTR: 6.2% (baseline: 5%)                         │
│  ⚠️  Fallback rate: 2.1% (target: <1%)              │
│  ✅ Coverage: 94% (target: >90%)                     │
│                                                      │
│  Pricing Model:                                      │
│  ✅ Revenue lift: +12% vs rule-based                 │
│  ✅ Prediction error: MAPE = 8% (target: <10%)      │
│  ✅ Updates/hour: 600 (expected)                     │
│                                                      │
│  Fraud Model:                                        │
│  ✅ Latency: p99 = 42ms (target: <50ms)             │
│  ✅ TPR: 87% (catching real fraud)                   │
│  ⚠️  FPR: 3.2% (false positives - target: <2%)      │
│  📊 Manual review queue: 145 cases                   │
│                                                      │
│  Chatbot:                                            │
│  ✅ Cache hit rate: 79% (target: >75%)              │
│  ✅ Function call success: 94%                       │
│  ✅ Avg tokens: 220 (cost optimization working!)    │
│  ⚠️  Escalation rate: 12% (target: <10%)            │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**Alerts:**
- 🚨 Model latency > 200ms for 5 minutes
- 🚨 Recommendation CTR drops >20% (model degradation)
- 🚨 Fraud model FPR > 5% (too many false positives)
- 🚨 Feature store lag > 5 minutes
- 🚨 LLM API rate limit hit
- 🚨 Model drift detected (prediction distribution changed)

---

This hybrid architecture combines the **reliability of traditional systems** with the **intelligence of AI/ML**, providing the best user experience while maintaining system integrity!
